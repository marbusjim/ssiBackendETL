# Coneccion de sql server
spring:
  datasource:
    url: jdbc:sqlserver://\ASUS\SQL2012;database=ssi_database
    username: sa
    password: control
    platform: mssql
    driverClassName: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    hibernate:
      ddl-auto: update
    database: SQL_SERVER
    show-sql: true
#Port initialize
server:
  port: 8082

